name: Bygg, deploy til dev-gcp og lag release

on:
  push:
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENCE"
      - "CODEOWNERS"
    branches:
      - main
      - vedlegg

jobs:
  build:
    name: Bygg, deploy til dev-gcp og lag release
    permissions:
      packages: write
      contents: write
      id-token: write
    secrets: inherit
    uses: navikt/hm-workflows/.github/workflows/deploy-dev.yaml@main
    with:
      environment: 'dev-gcp:teamdigihot'
      manifest: .nais/nais-dev.yaml
      vars: .nais/dev-q2.json
      java-version: '25'
